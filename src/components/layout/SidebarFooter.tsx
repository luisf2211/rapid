"use client";

/**
 * Footer del sidebar — vacío ahora que Configuración está en la sección Sistema.
 * Se mantiene el componente por si se necesita agregar algo en el futuro.
 */
export function SidebarFooter() {
  return null;
}
